const path = require('path');

// Exporta os principais caminhos da estrutura do projeto
module.exports = {
  config: path.resolve(__dirname, 'src', 'config', 'database.js'), // Caminho para a configuração do banco de dados
  migrationsPath: path.resolve(__dirname, 'src', 'database', 'migrations'), // Caminho para as migrações
  seedersPath: path.resolve(__dirname, 'src', 'database', 'seeders'), // Caminho para os seeders
  modelsPath: path.resolve(__dirname, 'src', 'models') // Caminho para os modelos do Sequelize
};
