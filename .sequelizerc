const path = require('path');

// Exporta a configuração de caminhos para a estrutura do projeto
module.exports = {
  // Caminho para o arquivo de configuração do banco de dados
  config: path.resolve(__dirname, 'src', 'config', 'database.js'),

  // Caminho para as migrações do banco de dados
  migrationsPath: path.resolve(__dirname, 'src', 'database', 'migrations'),

  // Caminho para os seeders (scripts que preenchem o banco de dados com dados iniciais)
  seedersPath: path.resolve(__dirname, 'src', 'database', 'seeders'),

  // Caminho para os modelos (models) Sequelize do projeto
  modelsPath: path.resolve(__dirname, 'src', 'models'),
};
